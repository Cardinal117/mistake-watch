// Compatibility security invariants remain implemented in the watch modules:
// canAccessUploadedCatalogue; "No permission to access uploaded content";
// createUploadedAssetReference(asset.id); createUploadedPlaybackSession;
// createUploadedSessionReference(session.id).
export { WatchModeLayout } from "./watch/watch-mode-layout";
